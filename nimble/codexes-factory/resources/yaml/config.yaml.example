# This is an example configuration for streamlit-authenticator.
# 1. RENAME this file to 'config.yaml'.
# 2. GENERATE secure passwords for your users.
#    - You can use the included 'generate_keys.py' script from streamlit-authenticator.
#    - Example: `python -m streamlit_authenticator.generate_keys`
#    - Copy the generated hashed passwords into the 'password' fields below.
# 3. SET a unique and secure cookie key.

credentials:
  usernames:
    jsmith:
      email: jsmith@example.com
      name: John Smith
      password: 'hashed_password_for_jsmith' # Replace with a hashed password
      role: 'user'
    rroe:
      email: rroe@example.com
      name: Rebecca Roe
      password: 'hashed_password_for_rroe'  # Replace with a hashed password
      role: 'subscriber'
    admin:
      email: admin@example.com
      name: Admin User
      password: 'hashed_password_for_admin' # Replace with a hashed password
      role: 'admin'
cookie:
  expiry_days: 30
  key: 'replace_this_with_a_random_secret_key' # IMPORTANT: Generate a new secret key
  name: codexes_auth_cookie
pre-authorized:
  emails:
  - melsby@example.com