# card2dropkit

# takes Trillions card and prepares it for DropKit

def authenticate2dropkit(credentials):
    # authenticates with dropkit
    return token

def create_dropkit_json(card_df, card_image):
    # creates json in this format

    return json

def post_to_dropkit(json, image):
    # posts json to dropkit
    return status

def update_local_status(card_id, status):
    # updates local status
    return status

def main():
    # main function
    create_dropkit_json(card_df, card_image)
    post_to_dropkit(json, image)
    update_local_status(card_id, status)
    return

if __name__ == "main":
    main()
